model_config:
  spatial_dims: 2
  in_channels: 1
  out_channels: 3
  channels: [32, 64, 128, 256]
  strides: [2, 2, 2]
  num_res_units: 2
  norm: "batch"       # batch / instance / group
  act: "relu"         # relu / leakyrelu / prelu
  dropout: 0.1